# 项目协作规则

## 1. 项目规则

- 修改前必须读取相关文件。
- 发现文件矛盾时，必须一次性列出全部矛盾并询问用户。
- 不得改动用户未要求的功能。
- 新增依赖前，必须说明原因并取得用户同意。
- 最终回复必须列出：本次修改了什么、已如何验证、用户需要如何验证。
- 用户验证未完成前，不得将对应的人工验收或 Production 验收表述为通过。

## 2. 更新日志

- 只有推送 GitHub 时才更新项目唯一日志 `CHANGELOG.md`；开发过程中不更新，也不得另建 CHANGELOG。
- 每个版本或 Commit 只使用与改动相关的三级分类，例如 `产品功能`、`界面优化`、`安全修复`、`修复与质量`、`测试与文档`、`文档与规范`。
- 每个事项固定使用“**功能小标题** → `摘要：` → `涉及文件：`”；摘要说明问题、主要行为和边界，涉及文件紧跟该事项，删除文件标注“删除”。不得在条目末尾集中罗列全部文件。
